---
before_install:
  - gem install bundler --no-document
language: ruby
rvm:
  - 2.2.8
  - 2.3.5
  - 2.4.2
  - ruby-head
env:
  global:
    NOBENCHMARK=1
script: rake
matrix:
  allow_failures:
    - rvm: jruby-9.1.15.0
